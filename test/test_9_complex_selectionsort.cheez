int main()
{
    int[] arr = [5,4,3,2,1];
    int n = 5;
    // selection sort
    for (int i = 0; i < n - 1; i = i + 1)
    {
        int min = i;
        for (int j = i + 1; j < n; j = j + 1)
        {
            if (arr[j] < arr[min]) {
                min = j;
            } else {

            }

        }
        // swap
        int tmp = arr[i];
        arr[i] = arr[min];
        arr[min] = tmp;
    }
    // print
    for (int k = 0; k < n; k = k + 1)
    {
        printi(arr[k]);
    }
    
}

